function router(pathname,handlers)
{
    if (typeof handlers[pathname] == 'function')
    {
        return handlers[pathname]();
    }
    else
    {
        return "404 Pagina no encontrada";
    }
}
exports.router = router;